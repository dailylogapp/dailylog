spring:
  application:
    name: dailylog

  datasource:
    url: jdbc:mysql://localhost:3306/dailylog
    username: root
    password: CompuNueva1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
